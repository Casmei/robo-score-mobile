import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  rootContainer: {
    flexDirection: "column",
    alignItems: "center",
  },
  controlsContainer: {
    flexDirection: "row",
    alignItems: "center",
  },
  valueText: {
    fontSize: 40,
  },
});
